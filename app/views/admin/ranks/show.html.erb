<% for column in Rank.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @rank.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @rank %> |
<%= link_to 'Back', :action => 'list' %>
